-- id es una columna de tipo INT (entero), que se utiliza comúnmente para almacenar números enteros.
-- name es una columna de tipo VARCHAR(256), que se utiliza para almacenar cadenas de texto de longitud variable con un máximo de 256 caracteres.
CREATE TABLE IF NOT EXISTS 'first_table' (id INT, name 	VARCHAR(256));
